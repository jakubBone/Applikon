# Development profile - local development settings
# Credentials are loaded from environment variables (see .env)

# Show SQL in dev mode
spring.jpa.show-sql=true

# Flyway - allow clean in dev only (DANGEROUS - never enable in prod!)
spring.flyway.clean-disabled=false
