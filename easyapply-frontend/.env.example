# Frontend environment (local development without Docker).
# In Docker this value is overridden by a build arg from docker-compose.yml.
#
# Copy to .env:
#   cp .env.example .env

VITE_API_URL=http://localhost:8080/api
