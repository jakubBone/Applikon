# Frontend Environment (local development bez Dockera)
# W Dockerze ta wartosc jest nadpisywana przez build arg z docker-compose.yml
#
# Skopiuj do .env:
#   cp .env.example .env

VITE_API_URL=http://localhost:8080/api
